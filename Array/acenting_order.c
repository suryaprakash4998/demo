#include<stdio.h>
int main()
{
	int a[8]={3,2,6,4,7,9,5,10};
	int size,i,sum;
	printf("The given number is%d",a[8]);
	scanf("%d",&size);
	*i=a[8];
	for(i=0;i<size;i++)
	{
		if(a[i]==a[8])
		{
			if(a[i]<a[8])
			{
				if(a[i]>a[8])
				sum=*a[i];
			}
		}
	}
	
}
